server:
  port: 8081
spring:
  application:
    name: message-service
  cloud:
    nacos:
      server-addr: 139.159.133.183:8848
      config:
        file-extension: yml
        username: nacos
        password: nacos
        shared-configs: #公用配置文件
          - data-id: base-source.yml
            refresh: true #是否支持自动刷新#是否支持自动刷新
        extension-configs:
          - data-id: redis-source.yml
            refresh: true
          - data-id: jdbc-source.yml
              refresh: false
